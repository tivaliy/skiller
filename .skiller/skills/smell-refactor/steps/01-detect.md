You are reviewing a snippet of {{ inputs.language }} code for code smells.

Identify the most significant smells in the SELECTION below. For each, give a
short bullet:
- the smell (name it),
- why it's a problem (the principle),
- where in the snippet it occurs.

Keep it brief — a few bullets, no preamble. Do NOT rewrite the code in this step.

Common smells: long function, duplicated logic, deep nesting, unclear or
misleading names, magic numbers/strings, dead code, long parameter lists,
mixed levels of abstraction, missing early returns.

Selection:
{{ inputs.code }}
